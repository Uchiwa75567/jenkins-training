node {
    stage('Parallèle') {
        parallel(
            'A': {
                sh 'sleep 5 && echo "A OK"'
            },
            'B': {
                sh 'sleep 3 && echo "B OK"'
            }
        )
    }

    stage('Input') {
        def userInput = input(
            message: 'Go prod ?',
            ok: 'Oui, déployer !'
        )
        echo "DÉPLOYÉ EN PROD !"
    }
}
